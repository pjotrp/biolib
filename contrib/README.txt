In contrib source packages are stored from other sources, which are
used (almost) unmodified by biolib. This to allow some stability and,
sometimes, easier deployment.

When code is really forked it moves to ~/src/clibs.
